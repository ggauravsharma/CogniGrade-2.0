"""Provider-neutral grading domain helpers.

Nothing in this package may import a specific AI provider. Provider-specific
work (uploading files, calling a model) stays in the router/adapter layer.
"""
